## 1. Core Implementation

- [ ] 1.1 Throw `ValidationException` in `DeckService.createDeck()` when deck validation fails
- [ ] 1.2 Throw `ValidationException` in `DeckService.updateDeck()` when deck validation fails
- [ ] 1.3 Verify `DeckService.createDeck()` throws propagate correctly through `SeedDeckService.seedDecks()`

## 2. Verification

- [ ] 2.1 Run backend build (`mvn compile`)
- [ ] 2.2 Run backend tests (`mvn test`)
