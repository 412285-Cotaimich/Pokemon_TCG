## 1. Core Implementation

- [x] 1.1 Modify `onCardSelected()` to check current card quantity against the 4-copy limit before calling `facade.addCard()`
- [x] 1.2 Show a warning notification via `NotificationService` when the add is blocked

## 2. Verification

- [x] 2.1 Build passes (`ng build`)
